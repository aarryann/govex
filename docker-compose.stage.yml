version: '3.7'

services:
  apper:
    build:
      args:
        NODE_ENV: production
    environment:
      NODE_ENV: production
      MODE_ENV: staging
      PORT: 443
    expose: [443]
    extra_hosts:
      - 'databasehost:192.168.1.214'
    ports:
      - '443:443'
